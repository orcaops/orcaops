{
  "name": "@orcaops/docs",
  "version": "0.0.1",
  "license": "FSL-1.1-ALv2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm check:facts && vitepress build content",
    "check:facts": "node validate-doc-facts.mjs",
    "dev": "vitepress dev content",
    "preview": "vitepress preview content",
    "test": "node --test validate-built-links.test.mjs && pnpm check:facts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@orcaops/prettier-config": "workspace:*",
    "vitepress": "^1.6.4",
    "vitepress-plugin-llms": "^1.13.5"
  }
}
